<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('links.edit'), edit_book_path(@book) %> |
  <%= link_to t('links.back'), books_path %>
  <%= button_to t('links.delete'), book_path(@book, locale: I18n.locale), method: :delete %>
</nav>
