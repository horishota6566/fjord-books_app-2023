<p style="color: green"><%= notice %></p>

<h1><%= t('.heading') %></h1>

<div id="books">
  <% @books.each do |book| %>
    <%= render book %>
    <p>
      <%= link_to t('links.show'), book %>
    </p>
  <% end %>
</div>

<%= link_to t('links.new'), new_book_path %>
