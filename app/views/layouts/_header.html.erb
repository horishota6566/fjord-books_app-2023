<% if user_signed_in? %>
  <header>
    <nav>
      <%= link_to t('nav.books_app'), root_path %>
      <ul>
        <li><%= link_to t('nav.books'), root_path %></li>
        <li><%= link_to t('nav.users'), users_path %></li>
        <li><%= link_to t('nav.profile'), user_path(current_user) %></li>
        <li><%= link_to t('nav.logout'), destroy_user_session_path, data: { "turbo-method": :delete } %></li>
      </ul>
    </nav>
  </header>
<% end %>
