<h2>New Comment</h2>
<%= render 'comments/form', commentable: commentable, comment: comment %>

<% if comments.present? %>
  <h2>Comments</h2>
  <%= render comments %>
  <%= paginate comments %>
<% else %>
  <p>No comments yet.</p>
<% end %>
